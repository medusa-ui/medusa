git pull

#verify all works
cd medusa-ui
mvn clean install
cd ../medusa-sample-implementation
mvn clean install

#release
cd ../medusa-ui
mvn versions:set -DnewVersion="0.3.7"
git add .
git commit -m "Release 0.3.7"
git tag 0.3.7
git push --tags

mvn clean deploy -DskipTests=true -Pnexus-staging

#post-release versioning fix
mvn versions:set -DnewVersion="0.3.8-SNAPSHOT"
cd ../medusa-sample-implementation
mvn versions:set -DnewVersion="0.3.8-SNAPSHOT"
cd ..

git add .
git status
git commit -m "Release 0.3.7 - Version update from 0.3.7 to 0.3.8-SNAPSHOT"
git push

#See https://central.sonatype.org/publish/requirements/ for publishing requirements