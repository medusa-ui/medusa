git pull

#verify all works
cd medusa-ui
mvn clean install
cd ../medusa-sample-implementation
mvn clean install

#release
cd ../medusa-ui
mvn versions:set -DnewVersion="0.3.5"
git add .
git commit -m "Release 0.3.5"
git tag 0.3.5
git push --tags

mvn clean deploy -DskipTests=true -Pnexus-staging

#post-release versioning fix
mvn versions:set -DnewVersion="0.3.6-SNAPSHOT"
cd ../medusa-sample-implementation
mvn versions:set -DnewVersion="0.3.6-SNAPSHOT"
cd ..

git add .
git commit -m "Release 0.3.5 - Version update from 0.3.5 to 0.3.6-SNAPSHOT"
git push

#See https://central.sonatype.org/publish/requirements/ for publishing requirements